{
  "name": "webgpu",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.51",
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "wgpu-matrix": "^3.4.0"
  }
}
